# リファクタリング進捗状況（v1.0.0）

## 1. 初回調査で洗い出した問題

### 最優先で対応したい問題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| バリデーション処理がAdd/EditのViewModelに重複しており、テストが不足している | 仕様変更時に画面ごとの差異や回帰が発生しやすい | 解決済み |
| Add/Editで入力状態・復元処理が重複している | 同じ修正を複数箇所へ反映する必要があり、SavedStateHandle復元時の不具合につながる | 解決済み |
| 編集画面の復元値がDBから再取得した値で上書きされる | プロセス再生成後にユーザーの編集中データを失う可能性がある | 解決済み |
| `VoiceRepository`が`CancellationException`を通常エラーへ変換する | キャンセルが上位へ伝播せず、キャンセル後にエラー表示や後続処理が走る可能性がある | 解決済み |
| `AudioPlayerManager`が独自の`CoroutineScope`を生成し、`play()`が再生ループ開始直後に成功を返す | 呼び出し元のライフサイクルから処理が外れ、再生中の失敗を呼び出し元へ返せない | 解決済み |

### 既知のUI課題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| UI表示文字列がKotlinコードへハードコードされている | 文言管理が分散し、再利用・検索・Lint対応が難しい | 解決済み |
| TrackAdd/TrackEdit画面のComposableが重複している | UI変更時の二重修正と表示差異につながる | 解決済み |
| 各画面で同様の`Scaffold`・`TopAppBar`を構築している | 共通スタイルやナビゲーションボタンの変更が分散する | 解決済み |
| TrackEditor固有Composableの配置と公開範囲が不明瞭 | 汎用Componentと画面固有UIの責務が分かりにくい | 解決済み |
| 一覧・下書き・再生画面でToastイベント購読処理が重複している | 副作用処理の修正が複数画面へ分散する | 未対応 |
| `collectAsState()`を使用しており、ライフサイクルに応じた収集停止が明示されていない | 画面が非表示でもFlow収集が継続する可能性がある | 解決済み |

### データ・非同期処理の問題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| Add/Update UseCaseで音声生成・進捗通知・保存処理が重複している | エラー処理や進捗仕様の変更時に差異が生まれやすい | 解決済み |
| VOICEVOX生成状況のポーリングにタイムアウトがない | APIが準備完了を返さない場合、キャンセルされるまで処理が継続する | 解決済み |
| `VoiceRepository.lastSynthesizeTimeMillis`が共有可変状態で同期されていない | 並行呼び出し時にレート制御が競合する可能性がある | 解決済み |
| `TrackRepository`が空一覧を`TrackNotFound`エラーとして返す | 正常な空状態とデータ取得失敗をUI側で区別しにくい | 解決済み |
| `MainActivity`内でRepository・UseCase・ViewModel生成が重複している | 依存関係の共有、差し替え、単体テストが難しい | 解決済み |
| Repository・UseCase・非同期処理のテストが不足している | キャンセル、API失敗、DB失敗など重要な分岐を保証できない | 一部解決済み |

## 2. Codexが推奨した着手順

1. バリデーションのテスト作成と`TrackValidator`分離
2. `TrackEditorUiState`とAdd/Edit ViewModelの共通部分整理
3. Add/Edit画面の共通Composable化
4. ハードコード文字列の`strings.xml`移行
5. データ・非同期処理のキャンセルとエラー伝播の改善
6. Add/Update UseCaseの共通処理整理とテスト追加
7. `MainActivity`に集中している依存生成・Navigation定義の整理

## 3. 推奨する着手順の進捗

| 順番 | タスク | 状況 | 主な成果 |
| --- | --- | --- | --- |
| 1 | バリデーションのテスト作成と`TrackValidator`分離 | 完了 | Validator分離、`Track.MAX_INTERVAL`制約、テスト追加 |
| 2 | Editor StateとViewModel共通部分整理 | 完了 | `TrackEditorUiState`・`TrackEditorStateHolder`導入、復元不具合修正、テスト追加 |
| 3 | Add/Edit画面の共通Composable化 | 完了 | `TrackEditorScreenContent`へ共通化、画面固有Composableを同一ファイルへ整理 |
| 4 | ハードコード文字列の`strings.xml`移行 | 完了 | 表示文言・アクセシビリティ文言・フォーマット文字列を移行 |
| 5 | データ・非同期処理の改善 | 完了 | キャンセル伝播、VOICEVOXタイムアウト、レート制御、保存キャンセル挙動を整理 |
| 6 | Add/Update UseCase共通化とテスト | 完了 | `TrackVoiceSynthesisService`を導入し、音声生成処理と進捗通知を共通化 |
| 7 | 依存生成・Navigation整理 | 完了 | `AppContainer`、`IkaranpuApp`、`IkaranpuNavHost`、共通transitionへ分離 |

推奨順の進捗は、7段階中7段階完了です。

## 4. その他で着手したこと

- 全画面で重複していた`Scaffold`と`TopAppBar`を`AppScaffold`へ共通化した。
- `TrackEditor.kt`を廃止し、画面固有UIを`TrackEditorScreen.kt`へ集約した。
- 未使用だったStateプロパティを削除し、音声生成進捗をNullableな状態へ統合した。
- 最後の読み上げテキスト削除時の状態変更をScreenからStateHolderへ移した。
- 自動生成された不要なサンプルテストを削除した。
- ユニットテストの日本語命名ルールを統一した。
- `PluralsCandidate`を`track_interval`だけ個別抑制した。
- `strings.xml`内の三点リーダーをAndroid推奨表記へ修正した。
- 未使用カラーリソース7件を削除した。
- キャラクター画像2件を`drawable`から`drawable-nodpi`へ移動した。
- Lint警告を75件から62件へ削減した。
- VOICEVOX生成タイムアウト用に`Error.VoiceSynthesisTimeout`を追加し、専用Toast文言を`strings.xml`へ追加した。
- Add/Edit保存成功後、画面遷移中の`ON_STOP`でキャンセル処理が走り、画面が一瞬enableになる不具合を修正した。
- `AudioPlayerManager.play()`を呼び出し元のCoroutineに紐づくsuspend処理へ変更し、再生JobをViewModel側で所有する形に整理した。
- `MainActivity`からNavigation定義と依存生成を分離し、Activityをアプリ起動処理だけに近づけた。
- Room Gradle Pluginとschema出力先を設定し、ファーストリリース時点の`version = 1` schema JSONをGit管理できる状態にした。
- Room schema export先未設定のビルドwarningを解消した。
- `TrackRepository`の通常・下書き一覧取得で、空一覧を`Ok(emptyList())`として返すように変更した。
- 一覧・下書き画面で、正常な空状態とDB取得エラーを区別して表示するようにした。
- `TrackRepositoryTest`を追加し、空一覧・Entity/Model変換・DAO委譲をテストで固定した。
- 全6画面の`collectAsState()`を`collectAsStateWithLifecycle()`へ移行した。

## 5. 問題の解決状況

### 最優先で対応したい問題

- 解決済み: 5件
- 未対応: 0件
- 入力・復元・バリデーション・キャンセル伝播・音声再生Coroutineの主要課題は解決した。

### 既知のUI課題

- 解決済み: 5件
- 未対応: 1件
- Add/Edit共通化、Scaffold共通化、文字列リソース化、画面固有Composableの配置整理まで完了した。
- Flow収集をライフサイクル対応へ移行した。
- Toastイベント購読処理の共通化は、追加改善候補として残っている。

### データ・非同期処理の問題

- 解決済み: 5件
- 一部解決済み: 1件
- 未対応: 0件
- VOICEVOX生成、レート制御、UseCase共通化、MainActivity依存生成分離は完了した。
- 空一覧を正常状態として扱うRepository/UI設計へ変更した。
- Repository・UseCase・非同期処理のテストは追加されたが、DB例外系などには追加余地がある。

## 6. 未対応の問題

ファーストリリース前のリファクタリング対応として、以下を残課題として扱う。

### DB・リリース準備

- 対応済み。Roomのschema export先を設定し、`version = 1`のDB schema JSONを出力した。

### 既知のUI課題

- 一覧・下書き・再生画面の共通EventEffectを検討する。
- 各Screenで使われている共通paddingと、Editor内の主要なSpacer値を定数化する。

### データ・非同期処理の問題

- Repository層のDB例外系テスト追加を検討する。
- `PlayTrackUseCase`を具象`SettingsRepository`ではなく`ISettingsRepository`依存へ変更する。

### Lint・保守作業

- Compose関連Lintを正しく実行できるよう、Android Gradle Pluginと依存関係を計画的に更新する。
- `app_icon_squid.xml`の長いVector Path警告は、表示品質と性能を確認したうえで対応要否を判断する。
- 文字化けしている既存コメントを、必要なものだけ日本語または英語で書き直す。

### 対応しない判断

- ViewModel生成処理のさらなる共通化は、今後ViewModelが増える予定がないため対応しない。
- Add/Update UseCaseのさらなる共通化は、現状の`TrackVoiceSynthesisService`切り出しで十分整理できているため対応しない。

## 7. 次回行う作業

初回調査で推奨した7段階のリファクタリングは完了した。

残課題のうち、以下は完了した。

- Room schema export設定 + warning解消
- `TrackRepository`の空一覧を`Ok(emptyList())`として扱う修正とRepositoryテスト追加
- `collectAsStateWithLifecycle()`への移行

次回以降の推奨順は以下とする。

1. padding / Spacer値の定数化
2. 一覧・下書き・再生画面の共通`UiEventEffect`化
3. `PlayTrackUseCase`の`ISettingsRepository`依存化
4. Navigation引数不正時の扱い整理
5. Repository層のDB例外系テスト追加の検討
6. 文字化けコメント整理とLint残件確認

この残課題の対応が完了したら、リファクタリング対応はいったん区切り、ファーストリリース準備へ進む予定。
