# リファクタリング進捗状況

- 対象日: 2026年6月7日（日本時間）
- 対象ブランチ: `master`
- 検証: `testDebugUnitTest`、`assembleDebug`、`lintDebug` 成功
- ユニットテスト: 22件成功

## 1. 初回調査で洗い出した問題

### 最優先で対応したい問題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| バリデーション処理がAdd/EditのViewModelに重複しており、テストが不足している | 仕様変更時に画面ごとの差異や回帰が発生しやすい | 解決済み |
| Add/Editで入力状態・復元処理が重複している | 同じ修正を複数箇所へ反映する必要があり、SavedStateHandle復元時の不具合につながる | 解決済み |
| 編集画面の復元値がDBから再取得した値で上書きされる | プロセス再生成後にユーザーの編集中データを失う可能性がある | 解決済み |
| `VoiceRepository`が`CancellationException`を通常エラーへ変換する | キャンセルが上位へ伝播せず、キャンセル後にエラー表示や後続処理が走る可能性がある | 未対応 |
| `AudioPlayerManager`が独自の`CoroutineScope`を生成し、`play()`が再生ループ開始直後に成功を返す | 呼び出し元のライフサイクルから処理が外れ、再生中の失敗を呼び出し元へ返せない | 未対応 |

### 既知のUI課題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| UI表示文字列がKotlinコードへハードコードされている | 文言管理が分散し、再利用・検索・Lint対応が難しい | 解決済み |
| TrackAdd/TrackEdit画面のComposableが重複している | UI変更時の二重修正と表示差異につながる | 解決済み |
| 各画面で同様の`Scaffold`・`TopAppBar`を構築している | 共通スタイルやナビゲーションボタンの変更が分散する | 解決済み |
| TrackEditor固有Composableの配置と公開範囲が不明瞭 | 汎用Componentと画面固有UIの責務が分かりにくい | 解決済み |
| 一覧・下書き・再生画面でToastイベント購読処理が重複している | 副作用処理の修正が複数画面へ分散する | 未対応 |
| `collectAsState()`を使用しており、ライフサイクルに応じた収集停止が明示されていない | 画面が非表示でもFlow収集が継続する可能性がある | 未対応 |

### データ・非同期処理の問題

| 問題 | 修正すべき理由 | 現在の状況 |
| --- | --- | --- |
| Add/Update UseCaseで音声生成・進捗通知・保存処理が重複している | エラー処理や進捗仕様の変更時に差異が生まれやすい | 未対応 |
| VOICEVOX生成状況のポーリングにタイムアウトがない | APIが準備完了を返さない場合、キャンセルされるまで処理が継続する | 未対応 |
| `VoiceRepository.lastSynthesizeTimeMillis`が共有可変状態で同期されていない | 並行呼び出し時にレート制御が競合する可能性がある | 未対応 |
| `TrackRepository`が空一覧を`TrackNotFound`エラーとして返す | 正常な空状態とデータ取得失敗をUI側で区別しにくい | 未対応 |
| `MainActivity`内でRepository・UseCase・ViewModel生成が重複している | 依存関係の共有、差し替え、単体テストが難しい | 未対応 |
| Repository・UseCase・非同期処理のテストが不足している | キャンセル、API失敗、DB失敗など重要な分岐を保証できない | 未対応 |

## 2. 推奨する着手順

1. バリデーションのテスト作成と`TrackValidator`分離
2. `TrackEditorUiState`とAdd/Edit ViewModelの共通部分整理
3. Add/Edit画面の共通Composable化
4. ハードコード文字列の`strings.xml`移行
5. データ・非同期処理のキャンセルとエラー伝播の改善
6. Add/Update UseCaseの共通処理整理とテスト追加
7. `MainActivity`に集中している依存生成・Navigation定義の整理

## 3. 推奨する着手順の進捗

| 順番 | タスク | 状況 | 本日の主な成果 |
| --- | --- | --- | --- |
| 1 | バリデーションのテスト作成と`TrackValidator`分離 | 完了 | Validator分離、`Track.MAX_INTERVAL`制約、テスト追加 |
| 2 | Editor StateとViewModel共通部分整理 | 完了 | `TrackEditorUiState`・`TrackEditorStateHolder`導入、復元不具合修正、テスト追加 |
| 3 | Add/Edit画面の共通Composable化 | 完了 | `TrackEditorScreenContent`へ共通化、画面固有Composableを同一ファイルへ整理 |
| 4 | ハードコード文字列の`strings.xml`移行 | 完了 | 表示文言・アクセシビリティ文言・フォーマット文字列を移行 |
| 5 | データ・非同期処理の改善 | 未着手 | 次回着手候補 |
| 6 | Add/Update UseCase共通化とテスト | 未着手 | - |
| 7 | 依存生成・Navigation整理 | 未着手 | - |

推奨順の進捗は、7段階中4段階完了です。

## 4. その他で着手したこと

- 全画面で重複していた`Scaffold`と`TopAppBar`を`AppScaffold`へ共通化した。
- `TrackEditor.kt`を廃止し、画面固有UIを`TrackEditorScreen.kt`へ集約した。
- 未使用だったStateプロパティを削除し、音声生成進捗をNullableな状態へ統合した。
- 最後の読み上げテキスト削除時の状態変更をScreenからStateHolderへ移した。
- 自動生成された不要なサンプルテストを削除した。
- ユニットテストの日本語命名ルールを統一した。
- `PluralsCandidate`を`track_interval`だけ個別抑制した。
- `strings.xml`内の三点リーダーをAndroid推奨表記へ修正した。
- 未使用カラーリソース7件を削除した。
- キャラクター画像2件を`drawable`から`drawable-nodpi`へ移動した。
- Lint警告を75件から62件へ削減した。

## 5. 問題の解決状況

### 最優先で対応したい問題

- 解決済み: 3件
- 未対応: 2件
- 入力・復元・バリデーション周辺の最優先課題は解決した。
- 次の重点領域はキャンセル処理と音声再生Coroutineである。

### 既知のUI課題

- 解決済み: 4件
- 未対応: 2件
- Add/Edit共通化、Scaffold共通化、文字列リソース化まで完了した。

### データ・非同期処理の問題

- 解決済み: 0件
- 未対応: 6件
- 本日はUI・入力状態の整理を優先したため、本格的な対応は次回以降となる。

## 6. 未対応の問題

### 最優先で対応したい問題

- `VoiceRepository`でキャンセル例外を握りつぶさず再送出する。
- `AudioPlayerManager`の独自`CoroutineScope`を廃止し、再生処理の所有者・終了・失敗通知を明確にする。

### 既知のUI課題

- 一覧・下書き・再生画面の共通EventEffectを検討する。
- `collectAsStateWithLifecycle()`への移行を検討する。

### データ・非同期処理の問題

- Add/Update UseCaseの音声生成・保存フローを共通化する。
- VOICEVOXポーリングへタイムアウトを追加する。
- 音声生成リクエストのレート制御を並行実行に対して安全にする。
- 空一覧を正常状態として扱えるRepository/UI設計へ変更する。
- `MainActivity`から依存生成を分離する。
- Repository・UseCase・非同期処理のテストを追加する。

### Lint・保守作業

- Compose関連Lintを正しく実行できるよう、Android Gradle Pluginと依存関係を計画的に更新する。
- `app_icon_squid.xml`の長いVector Path警告は、表示品質と性能を確認したうえで対応要否を判断する。

## 7. 次回行う作業

次回は、推奨する着手順の5番目である「データ・非同期処理のキャンセルとエラー伝播の改善」から開始する。

最初に`VoiceRepository.generateAndDownload()`のキャンセル伝播をテストで固定し、`CancellationException`をエラーへ変換しない実装へ修正する。その後、VOICEVOXポーリングのタイムアウトとレート制御を整理する。

続いて`AudioPlayerManager`の再生Jobを構造化し、呼び出し元が停止・失敗・終了を正しく扱えるAPIへ改善する。影響範囲が広いため、現在の再生仕様をテストで明文化してから段階的に変更する。
